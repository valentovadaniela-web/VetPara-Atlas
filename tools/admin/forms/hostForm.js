export function renderHostTab() {
    const container = document.getElementById('tab-host');
    container.innerHTML = `
        <div class="admin-placeholder">
            <p>🧩 Tento panel (Hostiteľ) zatiaľ nie je implementovaný.</p>
            <p style="font-size:0.9rem;color:#95a5a6;">Nasleduje v ďalšom kroku.</p>
        </div>
    `;
}